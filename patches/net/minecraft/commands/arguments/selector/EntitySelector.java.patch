--- a/net/minecraft/commands/arguments/selector/EntitySelector.java
+++ b/net/minecraft/commands/arguments/selector/EntitySelector.java
@@ -105,7 +_,7 @@
     }
 
     private void checkPermissions(CommandSourceStack p_121169_) throws CommandSyntaxException {
-        if (this.usesSelector && !p_121169_.hasPermission(2)) {
+        if (!p_121169_.bypassSelectorPermissions && this.usesSelector && (!net.neoforged.neoforge.common.CommonHooks.canUseEntitySelectors(p_121169_) || !p_121169_.hasPermission(2, "minecraft.command.selector"))) { // Neo // CraftBukkit // Paper - add bypass for selector perms
             throw EntityArgument.ERROR_SELECTORS_NOT_ALLOWED.create();
         }
     }
@@ -192,16 +_,16 @@
         this.checkPermissions(p_121167_);
         if (this.playerName != null) {
             ServerPlayer serverplayer2 = p_121167_.getServer().getPlayerList().getPlayerByName(this.playerName);
-            return serverplayer2 == null ? List.of() : List.of(serverplayer2);
+            return serverplayer2 == null || !canSee(p_121167_, serverplayer2) ? List.of() : List.of(serverplayer2); // Purpur - Hide hidden players from entity selector
         } else if (this.entityUUID != null) {
             ServerPlayer serverplayer1 = p_121167_.getServer().getPlayerList().getPlayer(this.entityUUID);
-            return serverplayer1 == null ? List.of() : List.of(serverplayer1);
+            return serverplayer1 == null || !canSee(p_121167_, serverplayer1) ? List.of() : List.of(serverplayer1); // Purpur - Hide hidden players from entity selector
         } else {
             Vec3 vec3 = this.position.apply(p_121167_.getPosition());
             AABB aabb = this.getAbsoluteAabb(vec3);
             Predicate<Entity> predicate = this.getPredicate(vec3, aabb, null);
             if (this.currentEntity) {
-                if (p_121167_.getEntity() instanceof ServerPlayer serverplayer3 && predicate.test(serverplayer3)) {
+                if (p_121167_.getEntity() instanceof ServerPlayer serverplayer3 && predicate.test(serverplayer3) && canSee(p_121167_, serverplayer3)) { // Purpur - Hide hidden players from entity selector
                     return List.of(serverplayer3);
                 }
 
@@ -211,11 +_,12 @@
                 List<ServerPlayer> list;
                 if (this.isWorldLimited()) {
                     list = p_121167_.getLevel().getPlayers(predicate, i);
+                    list.removeIf(entityplayer3 -> !canSee(p_121167_, entityplayer3)); // Purpur - Hide hidden players from entity selector
                 } else {
                     list = new ObjectArrayList<>();
 
                     for (ServerPlayer serverplayer : p_121167_.getServer().getPlayerList().getPlayers()) {
-                        if (predicate.test(serverplayer)) {
+                        if (predicate.test(serverplayer) && canSee(p_121167_, serverplayer)) { // Purpur - Hide hidden players from entity selector
                             list.add(serverplayer);
                             if (list.size() >= i) {
                                 return list;
@@ -274,4 +_,10 @@
     public static Component joinNames(List<? extends Entity> p_175104_) {
         return ComponentUtils.formatList(p_175104_, Entity::getDisplayName);
     }
+
+    // Purpur start - Hide hidden players from entity selector
+    private boolean canSee(CommandSourceStack sender, ServerPlayer target) {
+        return !org.purpurmc.purpur.PurpurConfig.hideHiddenPlayersFromEntitySelector || !(sender.getEntity() instanceof ServerPlayer player) || player.getBukkitEntity().canSee(target.getBukkitEntity());
+    }
+    // Purpur end - Hide hidden players from entity selector
 }
