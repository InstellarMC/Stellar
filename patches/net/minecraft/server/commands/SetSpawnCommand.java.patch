--- a/net/minecraft/server/commands/SetSpawnCommand.java
+++ b/net/minecraft/server/commands/SetSpawnCommand.java
@@ -67,12 +_,22 @@
     private static int setSpawn(CommandSourceStack p_138650_, Collection<ServerPlayer> p_138651_, BlockPos p_138652_, float p_138653_) {
         ResourceKey<Level> resourcekey = p_138650_.getLevel().dimension();
 
+        final Collection<ServerPlayer> actualTargets = new java.util.ArrayList<>(); // Paper - Add PlayerSetSpawnEvent
         for (ServerPlayer serverplayer : p_138651_) {
-            serverplayer.setRespawnPosition(resourcekey, p_138652_, p_138653_, true, false);
-        }
+            // Paper start - Add PlayerSetSpawnEvent
+            if (serverplayer.setRespawnPosition(resourcekey, p_138652_, p_138653_, true, false, com.destroystokyo.paper.event.player.PlayerSetSpawnEvent.Cause.COMMAND)) {
+                actualTargets.add(serverplayer);
+            }
+            // Paper end - Add PlayerSetSpawnEvent
+        }
+        // Paper start - Add PlayerSetSpawnEvent
+        if (actualTargets.isEmpty()) {
+            return 0;
+        }
+        // Paper end - Add PlayerSetSpawnEvent
 
         String s = resourcekey.location().toString();
-        if (p_138651_.size() == 1) {
+        if (actualTargets.size() == 1) { // Paper - Add PlayerSetSpawnEvent
             p_138650_.sendSuccess(
                 () -> Component.translatable(
                         "commands.spawnpoint.success.single",
@@ -80,20 +_,20 @@
                         p_138652_.getY(),
                         p_138652_.getZ(),
                         p_138653_,
-                        s,
-                        p_138651_.iterator().next().getDisplayName()
+                        p_138650_.getLevel().getDescription(), // Neo: Use dimension translation, if one exists
+                        actualTargets.iterator().next().getDisplayName() // Paper - Add PlayerSetSpawnEvent
                     ),
                 true
             );
         } else {
             p_138650_.sendSuccess(
                 () -> Component.translatable(
-                        "commands.spawnpoint.success.multiple", p_138652_.getX(), p_138652_.getY(), p_138652_.getZ(), p_138653_, s, p_138651_.size()
+                        "commands.spawnpoint.success.multiple", p_138652_.getX(), p_138652_.getY(), p_138652_.getZ(), p_138653_, s, actualTargets.size() // Paper - Add PlayerSetSpawnEvent
                     ),
                 true
             );
         }
 
-        return p_138651_.size();
+        return actualTargets.size(); // Paper - Add PlayerSetSpawnEvent
     }
 }
