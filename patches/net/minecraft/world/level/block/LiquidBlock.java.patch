--- a/net/minecraft/world/level/block/LiquidBlock.java
+++ b/net/minecraft/world/level/block/LiquidBlock.java
@@ -6,7 +_,6 @@
 import com.mojang.serialization.DataResult;
 import com.mojang.serialization.MapCodec;
 import com.mojang.serialization.codecs.RecordCodecBuilder;
-import com.mojang.serialization.codecs.RecordCodecBuilder.Instance;
 import java.util.Collections;
 import java.util.List;
 import java.util.Optional;
@@ -36,6 +_,7 @@
 import net.minecraft.world.phys.shapes.CollisionContext;
 import net.minecraft.world.phys.shapes.Shapes;
 import net.minecraft.world.phys.shapes.VoxelShape;
+import org.bukkit.craftbukkit.event.CraftEventFactory;
 
 public class LiquidBlock extends Block implements BucketPickup {
     private static final Codec<FlowingFluid> FLOWING_FLUID = BuiltInRegistries.FLUID
@@ -134,14 +_,33 @@
 
     @Override
     protected void onPlace(BlockState p_54754_, Level p_54755_, BlockPos p_54756_, BlockState p_54757_, boolean p_54758_) {
-        if (this.shouldSpreadLiquid(p_54755_, p_54756_, p_54754_)) {
-            p_54755_.scheduleTick(p_54756_, p_54754_.getFluidState().getType(), this.fluid.getTickDelay(p_54755_));
-        }
-    }
+        if (p_54755_.purpurConfig.tickFluids && !net.neoforged.neoforge.fluids.FluidInteractionRegistry.canInteract(p_54755_, p_54756_)) { // Purpur - Tick fluids config
+            p_54755_.scheduleTick(p_54756_, p_54754_.getFluidState().getType(), this.getFlowSpeed(p_54755_, p_54756_)); // Paper - Configurable speed for water flowing over lava
+        }
+    }
+
+    // Paper start - Configurable speed for water flowing over lava
+    public int getFlowSpeed(Level world, BlockPos pos) {
+        if (net.minecraft.core.registries.BuiltInRegistries.FLUID.wrapAsHolder(this.fluid).is(FluidTags.WATER)) {
+            if (isLava(world, pos.north(1))
+                || isLava(world, pos.south(1))
+                || isLava(world, pos.west(1))
+                || isLava(world, pos.east(1))
+            ) {
+                return world.paperConfig().environment.waterOverLavaFlowSpeed;
+            }
+        }
+        return this.fluid.getTickDelay(world);
+    }
+    private static boolean isLava(Level world, BlockPos blockPos) {
+        final FluidState fluidState = world.getFluidState(blockPos);
+        return fluidState != null && fluidState.is(FluidTags.LAVA);
+    }
+    // Paper end - Configurable speed for water flowing over lava
 
     @Override
     protected BlockState updateShape(BlockState p_54723_, Direction p_54724_, BlockState p_54725_, LevelAccessor p_54726_, BlockPos p_54727_, BlockPos p_54728_) {
-        if (p_54723_.getFluidState().isSource() || p_54725_.getFluidState().isSource()) {
+        if ((p_54726_.getWorldBorder().world == null || p_54726_.getWorldBorder().world.purpurConfig.tickFluids) && p_54723_.getFluidState().isSource() || p_54725_.getFluidState().isSource()) { // Purpur - Tick fluids config
             p_54726_.scheduleTick(p_54727_, p_54723_.getFluidState().getType(), this.fluid.getTickDelay(p_54726_));
         }
 
@@ -150,11 +_,12 @@
 
     @Override
     protected void neighborChanged(BlockState p_54709_, Level p_54710_, BlockPos p_54711_, Block p_54712_, BlockPos p_54713_, boolean p_54714_) {
-        if (this.shouldSpreadLiquid(p_54710_, p_54711_, p_54709_)) {
-            p_54710_.scheduleTick(p_54711_, p_54709_.getFluidState().getType(), this.fluid.getTickDelay(p_54710_));
+        if (p_54710_.purpurConfig.tickFluids && !net.neoforged.neoforge.fluids.FluidInteractionRegistry.canInteract(p_54710_, p_54711_)) { // Purpur - Tick fluids config
+            p_54710_.scheduleTick(p_54711_, p_54709_.getFluidState().getType(), this.getFlowSpeed(p_54710_, p_54711_)); // Paper - Configurable speed for water flowing over lava
         }
     }
 
+    @Deprecated // FORGE: Use FluidInteractionRegistry#canInteract instead
     private boolean shouldSpreadLiquid(Level p_54697_, BlockPos p_54698_, BlockState p_54699_) {
         if (this.fluid.is(FluidTags.LAVA)) {
             boolean flag = p_54697_.getBlockState(p_54698_.below()).is(Blocks.SOUL_SOIL);
@@ -163,14 +_,18 @@
                 BlockPos blockpos = p_54698_.relative(direction.getOpposite());
                 if (p_54697_.getFluidState(blockpos).is(FluidTags.WATER)) {
                     Block block = p_54697_.getFluidState(p_54698_).isSource() ? Blocks.OBSIDIAN : Blocks.COBBLESTONE;
-                    p_54697_.setBlockAndUpdate(p_54698_, block.defaultBlockState());
+                    // p_54697_.setBlockAndUpdate(p_54698_, block.defaultBlockState());
+                    if (CraftEventFactory.handleBlockFormEvent(p_54697_, p_54698_, block.defaultBlockState())) { // CraftBukkit
                     this.fizz(p_54697_, p_54698_);
+                    } // CraftBukkit
                     return false;
                 }
 
                 if (flag && p_54697_.getBlockState(blockpos).is(Blocks.BLUE_ICE)) {
-                    p_54697_.setBlockAndUpdate(p_54698_, Blocks.BASALT.defaultBlockState());
+                    // p_54697_.setBlockAndUpdate(p_54698_, Blocks.BASALT.defaultBlockState());
+                    if (CraftEventFactory.handleBlockFormEvent(p_54697_, p_54698_, Blocks.BASALT.defaultBlockState())) { // CraftBukkit
                     this.fizz(p_54697_, p_54698_);
+                    } // CraftBukkit
                     return false;
                 }
             }
