--- a/net/minecraft/world/entity/ai/sensing/NearestBedSensor.java
+++ b/net/minecraft/world/entity/ai/sensing/NearestBedSensor.java
@@ -53,11 +_,31 @@
                     return true;
                 }
             };
-            Set<Pair<Holder<PoiType>, BlockPos>> set = poimanager.findAllWithType(
-                    p_217819_ -> p_217819_.is(PoiTypes.HOME), predicate, p_26686_.blockPosition(), 48, PoiManager.Occupancy.ANY
-                )
-                .collect(Collectors.toSet());
-            Path path = AcquirePoi.findPathToPois(p_26686_, set);
+            // Paper start - optimise POI access
+            java.util.List<Pair<Holder<PoiType>, BlockPos>> poiposes = new java.util.ArrayList<>();
+            // don't ask me why it's unbounded. ask mojang.
+            io.papermc.paper.util.PoiAccess.findAnyPoiPositions(poimanager, type -> type.is(PoiTypes.HOME), predicate, p_26686_.blockPosition(), p_26685_.purpurConfig.villagerNearestBedSensorSearchRadius, PoiManager.Occupancy.ANY, false, Integer.MAX_VALUE, poiposes); // Purpur
+            // Stellar start - Process pathfinding asynchronously
+            if (dev.instellar.stellar.configuration.GlobalConfiguration.get().entities.asyncPathProcess.enabled) {
+                Path possible = AcquirePoi.findPathToPois(p_26686_, new java.util.HashSet<>(poiposes));
+                dev.kaiijumc.kaiiju.path.AsyncPathProcessor.awaitProcess(p_26686_, possible, path -> {
+                    if ((path == null || !path.canReach()) && this.triedCount < 5) {
+                        this.batchCache.long2LongEntrySet().removeIf(entry -> entry.getLongValue() < this.lastUpdate);
+                        return;
+                    }
+                    if (path == null) return;
+
+                    BlockPos pos = path.getTarget();
+                    Optional<Holder<PoiType>> poiType = poimanager.getType(pos);
+                    if (poiType.isEmpty()) return;
+
+                    p_26686_.getBrain().setMemory(MemoryModuleType.NEAREST_BED, pos);
+                });
+                return;
+            }
+            // Stellar end - Process pathfinding asynchronously
+            Path path = AcquirePoi.findPathToPois(p_26686_, new java.util.HashSet<>(poiposes));
+            // Paper end - optimise POI access
             if (path != null && path.canReach()) {
                 BlockPos blockpos = path.getTarget();
                 Optional<Holder<PoiType>> optional = poimanager.getType(blockpos);
