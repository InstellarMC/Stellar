--- a/net/minecraft/core/AxisCycle.java
+++ b/net/minecraft/core/AxisCycle.java
@@ -35,7 +_,14 @@
 
         @Override
         public Direction.Axis cycle(Direction.Axis p_121826_) {
-            return AXIS_VALUES[Math.floorMod(p_121826_.ordinal() + 1, 3)];
+            // Stellar start - lithium: math.fast_util - Avoid expensive array/modulo operations
+            return switch (p_121826_.ordinal()) {
+                case 0 -> Direction.Axis.Y;
+                case 1 -> Direction.Axis.Z;
+                case 2 -> Direction.Axis.X;
+                default -> throw new IncompatibleClassChangeError();
+            };
+            // Stellar end - lithium: math.fast_util - Avoid expensive array/modulo operations
         }
 
         @Override
@@ -56,7 +_,14 @@
 
         @Override
         public Direction.Axis cycle(Direction.Axis p_121837_) {
-            return AXIS_VALUES[Math.floorMod(p_121837_.ordinal() - 1, 3)];
+            // Stellar start - lithium: math.fast_util - Avoid expensive array/modulo operations
+            return switch (p_121837_.ordinal()) {
+                case 0 -> Direction.Axis.Z;
+                case 1 -> Direction.Axis.X;
+                case 2 -> Direction.Axis.Y;
+                default -> throw new IncompatibleClassChangeError();
+            };
+            // Stellar end - lithium: math.fast_util - Avoid expensive array/modulo operations
         }
 
         @Override
@@ -77,6 +_,16 @@
     public abstract AxisCycle inverse();
 
     public static AxisCycle between(Direction.Axis p_121800_, Direction.Axis p_121801_) {
-        return VALUES[Math.floorMod(p_121801_.ordinal() - p_121800_.ordinal(), 3)];
+        // Stellar start - Simplify and inline
+        if (p_121800_ == p_121801_) {
+            return NONE;
+        }
+        int delta = p_121801_.ordinal() - p_121800_.ordinal();
+        if (delta == 1 || delta == -2) {
+            return FORWARD;
+        }
+
+        return BACKWARD;
+        // Stellar end - Simplify and inline
     }
 }
