--- a/net/minecraft/core/BlockPos.java
+++ b/net/minecraft/core/BlockPos.java
@@ -48,15 +_,22 @@
     };
     private static final Logger LOGGER = LogUtils.getLogger();
     public static final BlockPos ZERO = new BlockPos(0, 0, 0);
-    private static final int PACKED_X_LENGTH = 1 + Mth.log2(Mth.smallestEncompassingPowerOfTwo(30000000));
-    private static final int PACKED_Z_LENGTH = PACKED_X_LENGTH;
-    public static final int PACKED_Y_LENGTH = 64 - PACKED_X_LENGTH - PACKED_Z_LENGTH;
-    private static final long PACKED_X_MASK = (1L << PACKED_X_LENGTH) - 1L;
-    private static final long PACKED_Y_MASK = (1L << PACKED_Y_LENGTH) - 1L;
-    private static final long PACKED_Z_MASK = (1L << PACKED_Z_LENGTH) - 1L;
-    private static final int Y_OFFSET = 0;
-    private static final int Z_OFFSET = PACKED_Y_LENGTH;
-    private static final int X_OFFSET = PACKED_Y_LENGTH + PACKED_Z_LENGTH;
+    // Paper start - Optimize Bit Operations by inlining
+    private static final int PACKED_X_LENGTH = 26;
+    private static final int PACKED_Z_LENGTH = 26;
+    public static final int PACKED_Y_LENGTH = 12;
+    private static final long PACKED_X_MASK = 67108863;
+    private static final long PACKED_Y_MASK = 4095;
+    private static final long PACKED_Z_MASK = 67108863;
+    private static final int Z_OFFSET = 12;
+    private static final int X_OFFSET = 38;
+    // Paper end - Optimize Bit Operations by inlining
+
+    // Purpur start
+    public BlockPos(net.minecraft.world.entity.Entity entity) {
+        super(entity.getBlockX(), entity.getBlockY(), entity.getBlockZ());
+    }
+    // Purpur end
 
     public BlockPos(int p_121869_, int p_121870_, int p_121871_) {
         super(p_121869_, p_121870_, p_121871_);
@@ -66,28 +_,29 @@
         this(p_121877_.getX(), p_121877_.getY(), p_121877_.getZ());
     }
 
+    public static long getAdjacent(int baseX, int baseY, int baseZ, Direction direction) { return asLong(baseX + direction.getStepX(), baseY + direction.getStepY(), baseZ + direction.getStepZ()); } // Paper
     public static long offset(long p_121916_, Direction p_121917_) {
         return offset(p_121916_, p_121917_.getStepX(), p_121917_.getStepY(), p_121917_.getStepZ());
     }
 
     public static long offset(long p_121911_, int p_121912_, int p_121913_, int p_121914_) {
-        return asLong(getX(p_121911_) + p_121912_, getY(p_121911_) + p_121913_, getZ(p_121911_) + p_121914_);
+        return asLong((int) (p_121911_ >> 38) + p_121912_, (int) ((p_121911_ << 52) >> 52) + p_121913_, (int) ((p_121911_ << 26) >> 38) + p_121914_); // Paper - simplify/inline
     }
 
     public static int getX(long p_121984_) {
-        return (int)(p_121984_ << 64 - X_OFFSET - PACKED_X_LENGTH >> 64 - PACKED_X_LENGTH);
+        return (int)(p_121984_ >> 38); // Paper - simplify/inline
     }
 
     public static int getY(long p_122009_) {
-        return (int)(p_122009_ << 64 - PACKED_Y_LENGTH >> 64 - PACKED_Y_LENGTH);
+        return (int)((p_122009_ << 52) >> 52); // Paper - simplify/inline
     }
 
     public static int getZ(long p_122016_) {
-        return (int)(p_122016_ << 64 - Z_OFFSET - PACKED_Z_LENGTH >> 64 - PACKED_Z_LENGTH);
+        return (int)((p_122016_ << 26) >> 38); // Paper - simplify/inline
     }
 
     public static BlockPos of(long p_122023_) {
-        return new BlockPos(getX(p_122023_), getY(p_122023_), getZ(p_122023_));
+        return new BlockPos((int)(p_122023_ >> 38), (int)((p_122023_ << 52) >> 52), (int)((p_122023_ << 26) >> 38)); // Paper - simplify/inline
     }
 
     public static BlockPos containing(double p_275310_, double p_275414_, double p_275737_) {
@@ -115,10 +_,7 @@
     }
 
     public static long asLong(int p_121883_, int p_121884_, int p_121885_) {
-        long i = 0L;
-        i |= ((long)p_121883_ & PACKED_X_MASK) << X_OFFSET;
-        i |= ((long)p_121884_ & PACKED_Y_MASK) << 0;
-        return i | ((long)p_121885_ & PACKED_Z_MASK) << Z_OFFSET;
+        return (((long) p_121883_ & (long) 67108863) << 38) | (((long) p_121884_ & (long) 4095)) | (((long) p_121885_ & (long) 67108863) << 12); // Paper - inline constants and simplify
     }
 
     public static long getFlatIndex(long p_122028_) {
@@ -156,55 +_,72 @@
     }
 
     public BlockPos above() {
-        return this.relative(Direction.UP);
+        return new BlockPos(this.getX(), this.getY() + 1, this.getZ()); // Paper - Perf: Optimize BlockPosition
     }
 
     public BlockPos above(int p_121972_) {
-        return this.relative(Direction.UP, p_121972_);
+        return p_121972_ == 0 ? this : new BlockPos(this.getX(), this.getY() + p_121972_, this.getZ()); // Paper - Perf: Optimize BlockPosition
     }
 
     public BlockPos below() {
-        return this.relative(Direction.DOWN);
+        return new BlockPos(this.getX(), this.getY() - 1, this.getZ()); // Paper - Perf: Optimize BlockPosition
     }
 
     public BlockPos below(int p_122000_) {
-        return this.relative(Direction.DOWN, p_122000_);
+        return p_122000_ == 0 ? this : new BlockPos(this.getX(), this.getY() - p_122000_, this.getZ()); // Paper - Perf: Optimize BlockPosition
     }
 
     public BlockPos north() {
-        return this.relative(Direction.NORTH);
+        return new BlockPos(this.getX(), this.getY(), this.getZ() - 1); // Paper - Perf: Optimize BlockPosition
     }
 
     public BlockPos north(int p_122014_) {
-        return this.relative(Direction.NORTH, p_122014_);
+        return p_122014_ == 0 ? this : new BlockPos(this.getX(), this.getY(), this.getZ() - p_122014_); // Paper - Perf: Optimize BlockPosition
     }
 
     public BlockPos south() {
-        return this.relative(Direction.SOUTH);
+        return new BlockPos(this.getX(), this.getY(), this.getZ() + 1); // Paper - Perf: Optimize BlockPosition
     }
 
     public BlockPos south(int p_122021_) {
-        return this.relative(Direction.SOUTH, p_122021_);
+        return p_122021_ == 0 ? this : new BlockPos(this.getX(), this.getY(), this.getZ() + p_122021_); // Paper - Perf: Optimize BlockPosition
     }
 
     public BlockPos west() {
-        return this.relative(Direction.WEST);
+        return new BlockPos(this.getX() - 1, this.getY(), this.getZ()); // Paper - Perf: Optimize BlockPosition
     }
 
     public BlockPos west(int p_122026_) {
-        return this.relative(Direction.WEST, p_122026_);
+        return p_122026_ == 0 ? this : new BlockPos(this.getX() - p_122026_, this.getY(), this.getZ()); // Paper - Perf: Optimize BlockPosition
     }
 
     public BlockPos east() {
-        return this.relative(Direction.EAST);
+        return new BlockPos(this.getX() + 1, this.getY(), this.getZ()); // Paper - Perf: Optimize BlockPosition
     }
 
     public BlockPos east(int p_122031_) {
-        return this.relative(Direction.EAST, p_122031_);
+        return p_122031_ == 0 ? this : new BlockPos(this.getX() + p_122031_, this.getY(), this.getZ()); // Paper - Perf: Optimize BlockPosition
     }
 
     public BlockPos relative(Direction p_121946_) {
-        return new BlockPos(this.getX() + p_121946_.getStepX(), this.getY() + p_121946_.getStepY(), this.getZ() + p_121946_.getStepZ());
+        // Paper start - Perf: Optimize BlockPosition
+        switch (p_121946_) {
+            case UP:
+                return new BlockPos(this.getX(), this.getY() + 1, this.getZ());
+            case DOWN:
+                return new BlockPos(this.getX(), this.getY() - 1, this.getZ());
+            case NORTH:
+                return new BlockPos(this.getX(), this.getY(), this.getZ() - 1);
+            case SOUTH:
+                return new BlockPos(this.getX(), this.getY(), this.getZ() + 1);
+            case WEST:
+                return new BlockPos(this.getX() - 1, this.getY(), this.getZ());
+            case EAST:
+                return new BlockPos(this.getX() + 1, this.getY(), this.getZ());
+            default:
+                return new BlockPos(this.getX() + p_121946_.getStepX(), this.getY() + p_121946_.getStepY(), this.getZ() + p_121946_.getStepZ());
+        }
+        // Paper end - Perf: Optimize BlockPosition
     }
 
     public BlockPos relative(Direction p_121948_, int p_121949_) {
@@ -541,9 +_,9 @@
         }
 
         public BlockPos.MutableBlockPos set(int p_122179_, int p_122180_, int p_122181_) {
-            this.setX(p_122179_);
-            this.setY(p_122180_);
-            this.setZ(p_122181_);
+            this.x = p_122179_; // Paper - Perf: Manually inline methods in BlockPosition
+            this.y = p_122180_; // Paper - Perf: Manually inline methods in BlockPosition
+            this.z = p_122181_; // Paper - Perf: Manually inline methods in BlockPosition
             return this;
         }
 
@@ -568,7 +_,20 @@
         }
 
         public BlockPos.MutableBlockPos setWithOffset(Vec3i p_122160_, Direction p_122161_) {
-            return this.set(p_122160_.getX() + p_122161_.getStepX(), p_122160_.getY() + p_122161_.getStepY(), p_122160_.getZ() + p_122161_.getStepZ());
+            // Stellar start - Simplify and inline
+            int x = p_122160_.getX();
+            int y = p_122160_.getY();
+            int z = p_122160_.getZ();
+            switch (p_122161_) {
+                case WEST -> --x;
+                case EAST -> ++x;
+                case DOWN -> --y;
+                case UP -> ++y;
+                case NORTH -> --z;
+                case SOUTH -> ++z;
+            }
+            return this.set(x, y, z);
+            // Stellar end - Simplify and inline
         }
 
         public BlockPos.MutableBlockPos setWithOffset(Vec3i p_122155_, int p_122156_, int p_122157_, int p_122158_) {
@@ -580,13 +_,29 @@
         }
 
         public BlockPos.MutableBlockPos move(Direction p_122174_) {
-            return this.move(p_122174_, 1);
+            // Stellar start - Simplify and inline
+            return switch (p_122174_) {
+                case UP -> this.setY(this.getY() + 1);
+                case DOWN -> this.setY(this.getY() - 1);
+                case NORTH -> this.setZ(this.getZ() - 1);
+                case SOUTH -> this.setZ(this.getZ() + 1);
+                case EAST -> this.setX(this.getX() + 1);
+                case WEST -> this.setX(this.getX() - 1);
+            };
+            // Stellar end - Simplify and inline
         }
 
         public BlockPos.MutableBlockPos move(Direction p_122176_, int p_122177_) {
-            return this.set(
-                this.getX() + p_122176_.getStepX() * p_122177_, this.getY() + p_122176_.getStepY() * p_122177_, this.getZ() + p_122176_.getStepZ() * p_122177_
-            );
+            // Stellar start - Simplify and inline
+            return switch (p_122176_) {
+                case UP -> this.setY(this.getY() + p_122177_);
+                case DOWN -> this.setY(this.getY() - p_122177_);
+                case NORTH -> this.setZ(this.getZ() - p_122177_);
+                case SOUTH -> this.setZ(this.getZ() + p_122177_);
+                case EAST -> this.setX(this.getX() + p_122177_);
+                case WEST -> this.setX(this.getX() - p_122177_);
+            };
+            // Stellar end - Simplify and inline
         }
 
         public BlockPos.MutableBlockPos move(int p_122185_, int p_122186_, int p_122187_) {
@@ -611,17 +_,17 @@
         }
 
         public BlockPos.MutableBlockPos setX(int p_175341_) {
-            super.setX(p_175341_);
+            this.x = p_175341_; // Paper - Perf: Manually inline methods in BlockPosition
             return this;
         }
 
         public BlockPos.MutableBlockPos setY(int p_175343_) {
-            super.setY(p_175343_);
+            this.y = p_175343_; // Paper - Perf: Manually inline methods in BlockPosition
             return this;
         }
 
         public BlockPos.MutableBlockPos setZ(int p_175345_) {
-            super.setZ(p_175345_);
+            this.z = p_175345_; // Paper - Perf: Manually inline methods in BlockPosition
             return this;
         }
 
